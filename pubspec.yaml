name: flutter_booking_application
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.17.0-266.5.beta <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  #core
  equatable: ^2.0.3

  #Animations
  lottie: ^1.3.0

  #cache

  hive_flutter: ^1.1.0

  #Google
  json_serializable: ^6.2.0
  url_launcher: ^6.1.0
  json_annotation: ^4.5.0

  #network
  dio: ^4.0.6

  #state
  provider: ^6.0.2
  flutter_bloc: ^8.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.1.10
  hive_generator: ^1.1.2
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/png/
    - assets/lottie/
